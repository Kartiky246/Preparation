document.querySelector('.className')

//  a method in JavaScript used to select and return the first element 
//  that matches a specified CSS selector within the document.

// can use various CSS selectors like class names 
// (.classname), IDs (#id), element types (element), attribute selectors ([attribute]),